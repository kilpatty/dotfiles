# Folder view configuration files
.DS_Store
Desktop.ini

# Files that might appear on external disks
.Spotlight-V100
.Trashes


#GPG things we don't want online
gnupg.symlink/pubring.gpg*
gnupg.symlink/secring.gpg*
gnupg.symlink/trustdb.gpg*
gnupg.symlink/random_seed*
gnupg.symlink/private-keys-v1.d*
gnupg.symlink/gpg-agent-info

#SSH things we don't want online
ssh.symlink/*
!ssh.symlink/config

#Don't commit local gitconfig
git/gitconfig.local.symlink
