import Ember from 'ember'
import config from '../config/environment'

export default Ember.Controller.extend({
  actions: {
    showTransactionDetails(activity) {
      this.transitionToRoute('transactions', activity.id)
    }
  }
})
